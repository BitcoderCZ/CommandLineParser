namespace CommandLineParser
{
    public static class HelpText
    {
        //public static string Generate(Type commandType)
        //{
        //    // required to get the default values
        //    ConsoleCommand command = ConsoleCommand.CreateInstance(commandType);
        //}
    }
}
